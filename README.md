# Веб-приложение "Книжный магазин" (2025)

Проект представляет собой полноценную платформу для покупки и аренды книг, реализованную на стеке Spring Boot 3.4.

## 📊 Подробный анализ выполненного проекта

1.  **Архитектура MVC:** Проект строго следует принципам разделения ответственности (Controller-Service-Repository), что упрощает его масштабирование.
2.  **Безопасность на уровне 2025 года:** Использование Spring Security 6 с BCrypt-шифрованием гарантирует защиту пользовательских данных.
3.  **Гибридная модель доступа:** Реализовано четкое разграничение прав доступа между обычными пользователями и администраторами.
4.  **Персистентность данных:** СУБД H2 в файловом режиме обеспечивает надежное хранение данных без необходимости установки внешнего сервера баз данных.
5.  **Автоматизация бизнес-логики:** Реализована система автоматических уведомлений по расписанию (`@Scheduled`), что минимизирует ручной труд администратора.
6.  **Динамический фронтенд:** Использование Thymeleaf позволило создать адаптивный интерфейс, реагирующий на статус аутентификации пользователя.
7.  **Современный DI:** Применение внедрения зависимостей через конструкторы (Constructor Injection) повышает стабильность и тестируемость кода.

## 🛠 Рекомендации по устранению выявленных ошибок

В ходе разработки были выявлены и решены следующие критические моменты:

*   **Циклические зависимости:** Для предотвращения ошибок инициализации бины конфигурации (как `PasswordEncoder`) должны быть вынесены в отдельный класс `AppConfig`.
*   **Конфликты имен методов:** Недопустимо переопределение финальных методов (например, `getClass()`) в сущностях JPA, так как это нарушает работу JVM.
*   **Синхронизация шаблонов:** Ошибки рендеринга страниц чаще всего вызваны отсутствием фрагментов (header/footer). Необходимо строго соблюдать структуру папок в `src/main/resources/templates`.
*   **Инициализация БД:** При использовании H2 важно следить за корректностью SQL-синтаксиса в `data.sql`, так как он может отличаться от MySQL.
*   **Связь с БД:** При переходе на внешние СУБД (MySQL) всегда следует проверять статус службы и настройки порта в `application.properties`.
*   **Валидация зависимостей:** Рекомендуется регулярно обновлять `pom.xml` и использовать кнопку "Reload All Maven Projects" для исключения ошибок поиска библиотек.

## 🚀 Запуск проекта
1. Убедитесь, что установлена JDK 17+.
2. Запустите класс `BookstoreApplication`.
3. Доступ к приложению: `http://localhost:8080`
4. Доступ к БД: `http://localhost:8080/h2-console`
